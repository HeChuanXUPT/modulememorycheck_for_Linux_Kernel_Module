modulememorycheck_for_Linux_Kernel_Module
=========================================

modulememorycheck is a framework for the LKM's dynamic memory. It can detect memory leak, overflow and can monitor each、memory access information
